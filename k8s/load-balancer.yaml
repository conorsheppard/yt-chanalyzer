apiVersion: v1
kind: Service
metadata:
  name: yt-chanalyzer-service
  labels:
    app: yt-chanalyzer
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    nodePort: 30000
  selector:
    app: yt-chanalyzer